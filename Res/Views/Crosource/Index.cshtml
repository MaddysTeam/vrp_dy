@using Res.Business
@{
	ViewBag.Title = "作品";
}
<div class="col-md-10 content-wrapper">
	<div class="row">
		<div class="col-lg-4 ">
			<ul class="breadcrumb">
				<li>
					<i class="fa fa-home"></i>
					<a href="@Url.Action("Index", "Home")">首页</a>
				</li>
				<li class="active">@ViewBag.Title</li>
			</ul>
		</div>
	</div>

	<!-- main -->
	<div class="content">
		<div class="main-header">
			<h2>@ViewBag.Title</h2>
            <em>按资源名称查询或者点击查看作品分类列表</em>
		</div>

		<div class="main-content">

			<div class="row">
				<div class="col-sm-12">
					<div class="col-sm-4">
						<div class="form-group">
							<form action="@Url.Action("Search")" name="form">
								<div class="input-group">
									<input type="text" name="d" class="search-field form-control" placeholder="请输入作品名称，作者名称，或者" />
									<a href="javascript:form.submit()" class="input-group-addon fa fa-search"></a>
								</div>
							</form>
						</div>
					</div>
					<div class="col-sm-8">
                        <a href="@Url.Action("Edit", "Crosource")" class="btn btn-custom-primary btn-md"><i class="fa fa-plus-square"></i> 添加新作品</a>
						@*<a href="@Url.Action("Report", "Crosource")" class="btn btn-default btn-md"><i class="fa fa-bar-chart-o"></i> 资源分布</a>*@
					</div>
				</div>
			</div>

			<hr />

			@*<div class="row">
				<div class="col-md-12">
					@{
						var types = APBplDef.ResPickListBpl.Cached(ThisApp.PLKey_ResourceType).Items;
						foreach (var domain in APBplDef.ResPickListBpl.Cached(ThisApp).Items)
						{
							<div class="col-md-6">
								<div class="widget">
									<div class="widget-header">
										<h3><i class="fa fa-th-large"></i> @domain.Name</h3>
									</div>
									<div class="widget-content">
										<ul style="padding-left: 20px">
											@foreach (var type in types) {
												if (type.StrengthenValue == domain.PickListItemId){
											<li>@Html.ActionLink(type.Name, "Category", new { ResourceType = type.PickListItemId, title = domain.Name + " > " + type.Name })</li>
												}
											}
										</ul>
									</div>
								</div>
							</div>
						}
					}
				</div>

			</div>*@

		</div>
		<!-- /main-content -->
	</div>
	<!-- /main -->
</div>
